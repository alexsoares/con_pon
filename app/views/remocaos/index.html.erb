  <br/>
<div class="botao">
  <div id="conteudo">
<table>
    <tr>
      <td colspan="5">
        <h1 align="center">Marcar / Desmarcar professores para remoção</h1>
      <hr>    
      </td>
    </tr>
    <br/>    
      <tr>
        <% if current_user.has_role?('admin') or current_user.has_role?('supervisao') or current_user.has_role?('planejamento') then%>
          <td>| <%= link_to 'Professores Aguardando Remocao', remocao_sim_remocaos_path %></td>
        <%end%>
        <td>| <%= link_to 'Professores que Solicitaram  Remocao', lista_remocao_remocaos_path %></td>
        <td>| <%= link_to 'Solicitação de Remoção', new_remocao_path, {:title => 'Antes de Cadastrar a remocao de um professor, verifique na listagem ao lado se a mesma ja não esta criada'} %></td>
        <%  if current_user.has_role?('admin') or current_user.has_role?('supervisao') or current_user.has_role?('direcao') or current_user.has_role?('planejamento') then%>
          <td>| <%= link_to_remote('Ficha Remoção', :update => "contents  ", :url => { :action => "relato_remocao"}) %></td>
        <%end%>
      </tr>
    </table>
          <div id="remocao"></div>
 </div>
 
 </div>


